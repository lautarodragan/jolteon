# Soundtracks Component

This is 100% a copy-paste of the library, with modifications.

The code is terrible, the UX is terrible, but It Works™.

TODO:
- Code: Deduplicate song List code by introducing a SongList component?
- Code: Deduplicate tree code by generalizing it? `group_by` field, or something?
- UX: an in-UI way to add albums to the Soundtracks collection, so `.jolt` files aren't the only way?